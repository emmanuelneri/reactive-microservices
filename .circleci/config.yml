version: 2.1

orbs:
  maven: circleci/maven@0.0.12

jobs:
  compile:
    docker:
      - image: circleci/openjdk:8
    steps:
      - checkout
      - run: mvn -B compile -DskipTests
      - save_cache:
          paths:
            - ~/.m2
          key: circleci-blueprint-microservices-{{ checksum "pom.xml" }}
  unit-test:
    docker:
      - image: circleci/openjdk:8
    steps:
      - checkout
      - restore_cache:
          key: circleci-blueprint-microservices-{{ checksum "pom.xml" }}
      - run: mvn -B test
  integration-test:
    machine:
      - image: circleci/openjdk:8
    steps:
      - checkout
      - restore_cache:
          key: circleci-blueprint-microservices-{{ checksum "pom.xml" }}
      - run: mvn -B verify

workflows:
  compile_tests:
    jobs:
      - compile
      - unit-test
      - integration-test